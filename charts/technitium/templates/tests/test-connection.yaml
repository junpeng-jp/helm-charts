{{- $port := (index .Values.networking.service.main.ports "http").port -}}
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "technitium.fullname" . }}-test-connection
  labels:
    {{- include "technitium.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command:
        - wget
        - --spider
        - -q
        - http://{{ include "technitium.fullname" . }}:{{ $port }}/
