homeAssistant:
  gitops:
    enabled: true
    ssh:
      knownHostsConfigMap: gitops-known-hosts
      secretName: ha-gitops-ssh
    repos:
      - name: ha-config
        url: "git@github.com:example/ha-config.git"
