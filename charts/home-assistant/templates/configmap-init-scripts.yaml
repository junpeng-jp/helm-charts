{{- if include "home-assistant.bootstrapEnabled" . }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "home-assistant.configmapName.initScripts" . }}
  labels:
    {{- include "home-assistant.labels" . | nindent 4 }}
data:
  bootstrap.sh: |-
    {{- .Files.Get "scripts/bootstrap.sh" | nindent 4 }}
{{- end }}
